using System;
using ThreadPool.TestClass;

namespace ThreadPool {
  class Program {
    static void Main(string[] args) {
      BeginThreadPool t = new BeginThreadPool();
      TaskTResult t2 = new TaskTResult();
    }
  }
}
